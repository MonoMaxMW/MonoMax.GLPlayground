<Window x:Class="MonoMax.CPQ.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:dock="http://schemas.xceed.com/wpf/xaml/avalondock"
        xmlns:dockctrl="clr-namespace:Xceed.Wpf.AvalonDock.Controls;assembly=Xceed.Wpf.AvalonDock"
        xmlns:ds="clr-namespace:MonoMax.CPQ.Helpers"
        xmlns:cal="http://www.caliburnproject.org"
        mc:Ignorable="d"
        Title="MonoMax.CPQ" Width="640" Height="480">
    <Grid>
        <Grid Panel.ZIndex="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>
            <ContentControl cal:View.Model="{Binding NodesViewModel}" />
        </Grid>
        <ContentControl cal:View.Model="{Binding SceneViewModel}" />
    </Grid>
</Window>

<UserControl x:Class="MonoMax.CPQ.Views.Documents.SceneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:gl="clr-namespace:MonoMaxGraphics;assembly=MonoMax.MonoMaxGraphics"
             mc:Ignorable="d" 
             d:DesignHeight="480" d:DesignWidth="640">
    <Grid>
        <Grid Panel.ZIndex="1">
            <StackPanel>
                <Grid Height="10" />
                <StackPanel Margin="3" VerticalAlignment="Top" HorizontalAlignment="Center" Orientation="Horizontal">
                    <Button Content="Load mock data" Command="{Binding LoadMockDataCmd}" />
                </StackPanel>
            </StackPanel>
        </Grid>
        <gl:GLControl x:Name="GLControl" Loaded="GLControl_Initialized">

        </gl:GLControl>
    </Grid>
</UserControl>
